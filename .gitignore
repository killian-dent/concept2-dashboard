# Secrets — never commit
.streamlit/secrets.toml

# Dense technical planning scratch (cross-project convention) — never commit
PLANNING.md

# Machine-specific Claude context — never commit
CLAUDE.md
CLAUDE.local.md
.claude/settings.local.json

# Local SQLite workout cache
data/

# Jujutsu (jj colocated with git) — never commit jj internals
.jj/

# Python
__pycache__/
*.pyc
*.pyo
.Python

# Virtual environments
.venv/
venv/
env/

# macOS
.DS_Store

# Editor
.idea/
.vscode/
